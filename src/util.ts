export enum ActiveMode { Design, Html, Preview }
export enum ToolbarOptions { Simple, Full }
export enum PasteOptions { Full, PlainText, MSWord }
export enum Direction {LTR,RTL}
export enum CaretPosition {Inherit,Before,After}
export class Dictionary<T> { [key: string]: T; }

